<?php

echo $before_widget; 

if ($galeria) {
    foreach ($galeria as $id) {
        $imagen = wp_get_attachment_image_src($id, 'thumbnail');
        printf('<img src="%s" />', $imagen[0]);
    }
}

echo $after_widget;
